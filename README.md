# Next.js 어드민 사이트

현대적이고 반응형인 Next.js 기반 어드민 패널입니다.

## 🚀 기술 스택

- **Next.js 15** (App Router)
- **TypeScript 5** - 타입 안정성
- **Tailwind CSS 4** - 유틸리티 기반 스타일링
- **shadcn/ui** - 고품질 UI 컴포넌트
- **Zustand** - 상태 관리
- **Supabase** - 백엔드 (PostgreSQL, 인증)
- **bcryptjs** - 비밀번호 해싱
- **PWA (Progressive Web App)** - 오프라인 지원 및 앱 설치 기능

## 📁 프로젝트 구조

```
src/
├── app/                    # Next.js App Router
│   ├── admin/             # 어드민 페이지들
│   │   ├── users/         # 사용자 관리
│   │   └── page.tsx       # 대시보드
│   └── api/               # API 라우트
├── components/            # React 컴포넌트
│   ├── admin/            # 어드민 전용 컴포넌트
│   ├── layout/           # 레이아웃 컴포넌트
│   └── ui/               # shadcn/ui 컴포넌트
├── lib/                  # 유틸리티 및 설정
│   ├── auth/             # 인증 관련
│   ├── db/               # 데이터베이스 설정
│   └── validations/      # 폼 검증 스키마
├── store/                # Zustand 스토어
├── types/                # TypeScript 타입 정의
└── hooks/                # 커스텀 훅
```

## 🛠️ 설치 및 실행

1. **의존성 설치**
   ```bash
   npm install
   ```

2. **환경변수 설정**

   **방법 1: 환경변수 파일 복사 (추천)**
   ```bash
   cp .env.example .env.local
   ```
   - `.env.local` 파일을 편집하여 실제 Supabase 값으로 변경
   - 자세한 설정 방법은 [ENVIRONMENT_SETUP.md](./ENVIRONMENT_SETUP.md) 참조

   **방법 2: 수동 설정**
   ```bash
   # .env.local 파일 생성
   touch .env.local
   ```
   다음 내용을 추가:
   ```env
   NEXT_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co
   NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here
   SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here
   ```

3. **개발 서버 실행**
   ```bash
   npm run dev
   ```

4. **브라우저에서 확인**
   ```
   http://localhost:3000
   ```

## 📋 주요 기능

### ✅ 구현 완료
- [x] Next.js 15 프로젝트 설정 (App Router)
- [x] TypeScript 설정
- [x] Tailwind CSS + shadcn/ui 설정
- [x] 반응형 어드민 레이아웃
- [x] 사이드바 네비게이션 (권한 기반 메뉴 필터링)
- [x] 대시보드 페이지
- [x] 사용자 관리 페이지 (CRUD)
- [x] 학생 관리 페이지 (CRUD)
- [x] 등/하원 조회 페이지
- [x] 학습 리포트 페이지
- [x] **학원비수납내역 관리 페이지** (CRUD)
  - 입금내역 추가/수정/삭제
  - 학생별 입금내역 조회
  - 금액 천단위 자동 포맷팅
  - 입금방법 선택 (무통장/카드)
  - 현금영수증 발행 여부 관리
- [x] 권한 관리 시스템
- [x] 통계 카드 컴포넌트
- [x] 사용자 테이블 컴포넌트
- [x] 상태 관리 (Zustand)
- [x] **PWA (Progressive Web App) 설정**
  - 오프라인 지원
  - 홈 화면 설치 기능
  - Service Worker 자동 생성
  - 프로덕션 빌드에서만 활성화

### 🚧 진행 예정
- [ ] 다크 모드 지원
- [ ] 국제화 (i18n)
- [ ] 엑셀 내보내기
- [ ] 차트 및 그래프
- [ ] 실시간 알림

## 🎨 UI 컴포넌트

shadcn/ui를 사용하여 다음 컴포넌트들이 포함되어 있습니다:
- Button, Card, Input, Label
- Table, Dropdown Menu
- Avatar, Badge
- Navigation Menu, Sidebar

## 📱 반응형 디자인

- 모바일: 햄버거 메뉴로 사이드바 토글
- 태블릿: 적응형 그리드 레이아웃
- 데스크톱: 고정 사이드바 + 메인 콘텐츠

## 📲 PWA (Progressive Web App)

이 프로젝트는 PWA로 구성되어 있어 모바일 앱처럼 설치하고 사용할 수 있습니다.

### 주요 기능
- ✅ **오프라인 지원**: 네트워크 연결이 없어도 기본 기능 사용 가능
- ✅ **홈 화면 설치**: 모바일/데스크톱 브라우저에서 앱으로 설치 가능
- ✅ **빠른 로딩**: Service Worker를 통한 캐싱으로 빠른 페이지 로딩
- ✅ **앱처럼 사용**: 독립적인 창에서 실행, 브라우저 UI 없이 사용

### 설정 정보
- **패키지**: `@ducanh2912/next-pwa` (Next.js 15 호환)
- **활성화 조건**: 프로덕션 빌드에서만 활성화 (개발 모드에서는 비활성화)
- **매니페스트**: `public/manifest.json`
- **아이콘**: `public/icon-192x192.png`, `public/icon-512x512.png`

### 사용 방법

#### 1. 프로덕션 빌드
```bash
# 프로덕션 빌드 생성
npm run build

# 프로덕션 모드로 실행
npm start
```

#### 2. PWA 테스트
- 프로덕션 모드로 실행 후 브라우저에서 접속
- 개발자 도구 > Application > Manifest에서 PWA 설정 확인
- 모바일 브라우저에서 "홈 화면에 추가" 옵션 확인

#### 3. 아이콘 재생성
```bash
npm run pwa:icons
```

### 주의사항
- ⚠️ **HTTPS 필수**: PWA는 HTTPS 환경에서만 정상 작동합니다
- ⚠️ **프로덕션 빌드**: 개발 모드(`npm run dev`)에서는 PWA가 비활성화됩니다
- 💡 **아이콘 커스터마이징**: 실제 프로덕션에서는 디자인된 아이콘 이미지로 교체하세요

## 🔧 개발 도구

- **ESLint** - 코드 품질 검사
- **Prettier** - 코드 포맷팅
- **TypeScript** - 정적 타입 검사
- **Sharp** - 이미지 처리 (PWA 아이콘 생성)

## 💰 학원비수납내역 관리

학원비 수납 내역을 관리하는 기능입니다.

### 주요 기능
- 입금내역 추가/수정/삭제
- 학생별 입금내역 조회
- 금액 천단위 자동 포맷팅 (예: 150,000원)
- 입금방법 선택 (무통장/카드)
- 현금영수증 발행 여부 관리

### 데이터베이스
- `payment` 테이블 사용
- `student` 테이블과 연동
- ENUM 타입으로 입금방법 관리

### 접근 경로
- 좌측 메뉴: **FINANCE > 학원비수납내역**

## 📝 다음 단계

1. **추가 기능**
   - 엑셀 내보내기
   - 월별/연도별 통계
   - 미납자 리스트

2. **고급 기능**
   - 실시간 알림
   - 데이터 필터링 및 검색 고도화
   - 차트 및 그래프

## 🤝 기여하기

1. Fork the Project
2. Create your Feature Branch (`git checkout -b feature/AmazingFeature`)
3. Commit your Changes (`git commit -m 'Add some AmazingFeature'`)
4. Push to the Branch (`git push origin feature/AmazingFeature`)
5. Open a Pull Request

## 📄 라이선스

이 프로젝트는 MIT 라이선스 하에 배포됩니다.